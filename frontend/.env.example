# Base URL of the backend API. The default below matches the backend's
# default port (8080) and the /api prefix used by all controllers.
VITE_API_URL=https://skillsphere-backend-vinay-gnhserd6fpcdfkfa.centralindia-01.azurewebsites.net/api
